<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <!--<item name="android:windowContentTransitions">true</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
    </style>

    <!-- Theme for login and register activities -->
    <style name="LoginTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:dialogTheme">@style/DialogLightTheme</item>
        <item name="android:alertDialogTheme">@style/AlertDialogLightTheme</item>
        <item name="colorButtonNormal">@color/accent</item>
    </style>

    <!-- Theme for ProgressDialogs in login and register -->
    <style name="DialogLightTheme" parent="Theme.AppCompat.Light.Dialog"></style>

    <!-- Theme for ProgressDialogs in login and register -->
    <style name="AlertDialogLightTheme" parent="Theme.AppCompat.Light.Dialog.Alert"></style>

    <style name="ErrorText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/inputErrorLogin</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
